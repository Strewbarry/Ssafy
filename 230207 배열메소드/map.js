// const array = [1,2,3,4,5,6];

// const result =array.map(data => {return data});
// const result2 =array.map(data => data+1);

// 각 value 의 제곱값을 가져다 배열만들기
const arr = [1,2,3,4,5];
const result = arr.map(data => data*data);
// 각 배열값의 길이를 가져다 배열만들기
const arr2 =["a","bcd","ef","g"];
const result2 = arr2.map(dat => dat.length);

